# Published-image overlay. Pin SIDEKICK_IMAGE_TAG to a release version or an explicit test tag.
services:
  sidekick:
    build: !reset null
    image: ghcr.io/stx-labs/signer-sidekick:${SIDEKICK_IMAGE_TAG:?Set SIDEKICK_IMAGE_TAG in .env}
